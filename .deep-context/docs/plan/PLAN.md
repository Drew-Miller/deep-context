# Implementation Plan

1. Bootstrap and validate the Codex plugin and deterministic CLI.
2. Prove repository import against Aftershock without source mutation.
3. Bootstrap Deep Context's own durable context and remove temporary handoff prose after coverage validation.
4. Harden task checkpointing and evidence-bound closure with integration tests.
5. Install the plugin from the personal marketplace and verify discovery in a fresh Codex task.

## Next milestone: DC-08

Desktop chat/task association is implemented and installed. Read doc/reports/DC-08-IMPLEMENTATION.md for exact gates, source checkpoint, recovery limits and the remaining real desktop acceptance sequence. Preserve the historical sequence above and source checkpoints.
