# Deep Context Development

Read `AGENTS.md`, `CONTEXT_MAP.md`, `PROJECT.md`, `docs/plan/TASKS.md`, and `docs/plan/STATUS.json`. The ledger contains the exact next safe action and supersedes scheduling prose elsewhere.

Source code is resolved through `repo/`. Canonical product context lives under `doc/`; implementation planning and verification evidence live under `docs/plan/`.
